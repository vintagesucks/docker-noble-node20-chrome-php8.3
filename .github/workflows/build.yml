name: Build

on:
  push:
    branches-ignore:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Build
        id: docker_build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          push: false
